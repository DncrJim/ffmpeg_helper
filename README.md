# ffmpeg_helper
bash script to simplify running command line ffmpeg tasks

Sections to implement

concatenate

trim (start and stop points)

crop (change pixel envelope)

re-encode
  at same resolution
  at different resolution

add additional code for selection of streams
